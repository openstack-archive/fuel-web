'use strict';

module.exports = function(grunt) {
    var _ = require('lodash-node');

    grunt.registerTask('trim-compressed-static', 'Remove unnecessary static files after compression', function() {
        console.log(1)
    });
};